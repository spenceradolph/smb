# SMB

An smb (wrapper) agent for Mythic. Great for LOTL.

### Local Development

```bash
git clone https://github.com/spenceradolph/smb
cd smb

python3 -m venv ./.venv
source ./.venv/bin/activate
pip install mythic_container
python3 ./Payload_Type/smb_agent/main.py
```
